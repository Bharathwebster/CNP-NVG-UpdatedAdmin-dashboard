import { Direction } from '../interfaces/direction';
import { Routines } from './domRoutines';
export declare class Padding {
    element: any;
    direction: Direction;
    routines: Routines;
    constructor(element: any, direction: Direction, routines: Routines);
    size: number;
    getEdge(): number;
}
